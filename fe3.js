// lesson3
// debug
// array 和 string

// 下标从 0 开始 下标代表的是偏移量
// 循环访问所有变量的操作 叫遍历
// log('log')
array.push()
string.slice()

// debug(调试/出错)
1. 不要慌
2. 使用 log 定位
      不知道代码是否执行
      不知道代码执行的时候数据是什么样的
          用 log 去显示数据的值

var test = fuction() {
  log('test begin')
}
